---
layout: post
title:  "数字图像处理程序汇总"
date:   2020-12-16 13:38:23 +0530
categories: C++
---

> 本笔记主要是服务于广东工业大学（GDUT）数字图像处理期末考试，内涵全部考试范围内的程序

[TOC]

### 01位图处理程序的最小系统

```c++
#include <afxwin.h> 
#include <afxext.h> 
#include <afxdisp.h>   
#include <afxdtctl.h>
#include "resource.h"

/*文档类*/
class CDoc: public CDocument
{	
	DECLARE_DYNCREATE(CDoc)
};
IMPLEMENT_DYNCREATE(CDoc, CDocument)
/*主帧窗口类*/
class CMainFrame : public CFrameWnd
{	
	DECLARE_DYNCREATE(CMainFrame)
};
IMPLEMENT_DYNCREATE(CMainFrame, CFrameWnd)

/*视图类*/
class CMyView : public CView
{
public:
	//区域一：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓

	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	DECLARE_MESSAGE_MAP()
	DECLARE_DYNCREATE(CMyView)
public:
	CMyView()
	{
	//区域二：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓


	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	}
	virtual ~CMyView(){}

	public:
	virtual void OnDraw(CDC* pDC){} 

	protected:                                                          

	afx_msg void On01();                                            

};                                                                  
IMPLEMENT_DYNCREATE(CMyView, CView)                       
BEGIN_MESSAGE_MAP(CMyView, CView)                       
	
	ON_COMMAND(ID01, On01)                                           
	
END_MESSAGE_MAP()                                                    
                                                                    
/*应用程序类*/                                                      
class CApp : public CWinApp{	virtual BOOL InitInstance(); };
BOOL CApp::InitInstance ()                             
{		CSingleDocTemplate* pDocTemplate= new CSingleDocTemplate(IDR_MENU1,RUNTIME_CLASS(CDoc),
													

RUNTIME_CLASS(CMainFrame),RUNTIME_CLASS(CMyView));
		AddDocTemplate(pDocTemplate);CCommandLineInfo cmdInfo;ParseCommandLine(cmdInfo);
		if (!ProcessShellCommand(cmdInfo))	return FALSE;            
		m_pMainWnd->ShowWindow(SW_SHOW);m_pMainWnd->UpdateWindow();  
		m_pMainWnd->SetWindowPos(NULL,0,0,800,600,0);                
		m_pMainWnd->SetWindowText("版本1.01.");
		return TRUE;                                                 
}                                                                    
CApp theApp;             
                                
//区域三：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓
void CMyView::On01() 
{

	CBitmap hbitmap;         //利用"位图类"声明一个标识符为"hbitmap"的变量
	BITMAP bm;               //利用"位图文件头结构体"声明一个标识符为"bm"的变量
    unsigned char *buffer;            //声明了一个8位整型数"buffer"指针
	int Hang,Lie,WidthBytes;       //声明行总数目，列总数目，一行象素占据的字节个数
    							//对于RGB图像而言，一个像素三字节

	hbitmap.DeleteObject();                                  //位图类的清空位图函数
	hbitmap.LoadBitmap (IDB_BITMAP2);                        //加载位图文件
	hbitmap.GetBitmap (&bm);     //将所加载的位图文件的头文件信息装载到标识符为"bm"的变量中
	Hang=bm.bmHeight,Lie=bm.bmWidth,WidthBytes=bm.bmWidthBytes;
    //值得注意的是unsigned char是一个无符号位的char，可以将其容量看为一个字节（1Byte）或者8bit
	buffer= new unsigned char[Hang*WidthBytes];    //buffer指向一个数组，这个数组的大小可以装下该图像的所有字节！！！！！
    											//注意是字节！！！
	//新开辟一段由上述三个变量决定个数的8位整形数组然后被buffer指针所指向
	hbitmap.GetBitmapBits (Hang*WidthBytes, buffer);   //录入buffer数组的位图数据

    //buffer[j*WidthBytes+i*4+k];

	int i,j;	//定义像素坐标变量
	int tempR,tempG,tempB;	//定义红绿蓝像素数据变量

    CDC *pDC;	//定义CDC类的指针变量
	pDC=GetDC();	//获得当前窗口描述设备对象的指针

 
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{	
             //WidthBytes/Lie = 每一个像素的字节数
             //i*(WidthBytes/Lie) = 每个像素字节数的起始索引，由于i在循环中退一位了，所以此时不用-1
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
             //一次循环读取一次RGB数据送入SetPixel方法生成像素点数据
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));
		}
	}
}
```

### 02 直方图

~~~c++
#include <afxwin.h> 
#include <afxext.h> 
#include <afxdisp.h>   
#include <afxdtctl.h>
#include "resource.h"

/*文档类*/
class CDoc: public CDocument
{	
	DECLARE_DYNCREATE(CDoc)
};
IMPLEMENT_DYNCREATE(CDoc, CDocument)
/*主帧窗口类*/
class CMainFrame : public CFrameWnd
{	
	DECLARE_DYNCREATE(CMainFrame)
};
IMPLEMENT_DYNCREATE(CMainFrame, CFrameWnd)

/*视图类*/
class CMyView : public CView
{
public:
	//区域一：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓

	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	DECLARE_MESSAGE_MAP()
	DECLARE_DYNCREATE(CMyView)
public:
	CMyView()
	{
	//区域二：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓


	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	}
	virtual ~CMyView(){}
	public:
	virtual void OnDraw(CDC* pDC){} 
	protected:                                                          
	afx_msg void On01();                                            
};                                                                  
IMPLEMENT_DYNCREATE(CMyView, CView)                       
BEGIN_MESSAGE_MAP(CMyView, CView)                                                  
	ON_COMMAND(ID01, On01)                                           
END_MESSAGE_MAP()                                                    
                                                                    
/*应用程序类*/                                                      
class CApp : public CWinApp{	virtual BOOL InitInstance(); };
BOOL CApp::InitInstance ()                             
{		CSingleDocTemplate* pDocTemplate= new CSingleDocTemplate(IDR_MENU1,RUNTIME_CLASS(CDoc),
													

RUNTIME_CLASS(CMainFrame),RUNTIME_CLASS(CMyView));
		AddDocTemplate(pDocTemplate);CCommandLineInfo cmdInfo;ParseCommandLine(cmdInfo);
		if (!ProcessShellCommand(cmdInfo))	return FALSE;            
		m_pMainWnd->ShowWindow(SW_SHOW);m_pMainWnd->UpdateWindow();  
		m_pMainWnd->SetWindowPos(NULL,0,0,800,600,0);                
		m_pMainWnd->SetWindowText("版本1.01.");
		return TRUE;                                                 
}                                                                    
CApp theApp;             
                                
//区域三：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓
void CMyView::On01() 
{

	CBitmap hbitmap;         //利用"位图类"声明一个标识符为"hbitmap"的变量
	BITMAP bm;               //利用"位图文件头结构体"声明一个标识符为"bm"的变量
    unsigned char *buffer;            //声明了一个8位整型数"buffer"指针
	int Hang,Lie,WidthBytes;       //声明行总数目，列总数目，一行象素占据的字节个数

	hbitmap.DeleteObject();                                  //位图类的清空位图函数
	hbitmap.LoadBitmap (IDB_BITMAP2);                        //加载位图文件
	hbitmap.GetBitmap (&bm);     //将所加载的位图文件的头文件信息装载到标识符为"bm"的变量中
	Hang=bm.bmHeight,Lie=bm.bmWidth,WidthBytes=bm.bmWidthBytes;
	buffer= new unsigned char[Hang*WidthBytes]; 	//buffer指向一个数组，这个数组的大小可以装下该图像的所有字节！！！！！
    											//注意是字节！！！
	//新开辟一段由上述三个变量决定个数的8位整形数组然后被buffer指针所指向
	hbitmap.GetBitmapBits (Hang*WidthBytes, buffer);   //录入buffer数组的位图数据

    //buffer[j*WidthBytes+i*4+k];

	int i,j;	\\定义像素坐标变量
	int tempR,tempG,tempB;	\\定义红绿蓝像素数据变量

    CDC *pDC;	\\定义CDC类的指针变量
	pDC=GetDC();	\\获得当前窗口描述设备对象的指针


    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));
		}
	}

	MessageBox("以下将RGB图转灰度图");
	int temp;						//定义整型变量，当前循环到的像素的灰度值会暂时赋值给这个变量
    unsigned char *buffer2;            //声明了一个8位整型数"buffer"指针，灰度图像的像素灰度数据将会存储在该指针指向的空间
	buffer2= new unsigned char[Hang*Lie];	//因为我们知道这个数组存储的数据是灰度值，灰度值是[0~255],只需要一个字节，因此该
    									 //数组存储的数据量大小为Hang*Lie*1
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];	//红[0~255]
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];	//绿[0~255]
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];	//蓝[0~255]
			temp=(tempR+tempG+tempB)/3;		//将RGB值相加除以3取平均值
    	    pDC->SetPixel(i,j,RGB(temp,temp,temp));	//将灰度值作为原来数组的RGB数据传入，因为RGB都是一个值，显示出来的颜色是一个灰色
            									 //原理参照RGB颜色模型说明
			buffer2[j*Lie+i]=temp;	// 向buffer这个装灰度图所有数据的数组装入计算好的灰度值
		}
	}

	int Zhifangtu[256];	//众所周知，直方图描述图像中具有该灰度级的像素的个数
    for(int k=0;k<256;k++)
		Zhifangtu[k]=0;	//先将这个直方图进行初始化，所有的元素赋0，防止数组未赋值的随机数干扰程序
	MessageBox("以下灰度图的直方图输出");
	for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			temp=buffer2[j*Lie+i];	//存储循环到的像素的灰度值
			Zhifangtu[temp]++;	//为相应的灰度级+1
		}
	}
	
    //	以下是简单的图像存储和位图移动
	FILE* fp=fopen("F://abc.txt","w");
	for(k=0;k<256;k++)
		fprintf(fp,"灰阶＝%d 次数=%d\n",k,Zhifangtu[k]);
	fclose(fp);

	for(k=0;k<256;k++)
	{
		pDC->MoveTo(Lie+20+k,Hang);
		pDC->LineTo(Lie+20+k,Hang-Zhifangtu[k]*0.1);
	}



}
~~~

### 03 点运算

~~~c++
#include <afxwin.h> 
#include <afxext.h> 
#include <afxdisp.h>   
#include <afxdtctl.h>
#include "resource.h"

/*文档类*/
class CDoc: public CDocument
{	
	DECLARE_DYNCREATE(CDoc)
};
IMPLEMENT_DYNCREATE(CDoc, CDocument)
/*主帧窗口类*/
class CMainFrame : public CFrameWnd
{	
	DECLARE_DYNCREATE(CMainFrame)
};
IMPLEMENT_DYNCREATE(CMainFrame, CFrameWnd)

/*视图类*/
class CMyView : public CView
{
public:
	//区域一：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓

	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	DECLARE_MESSAGE_MAP()
	DECLARE_DYNCREATE(CMyView)
public:
	CMyView()
	{
	//区域二：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓


	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	}
	virtual ~CMyView(){}
	public:
	virtual void OnDraw(CDC* pDC){} 
	protected:                                                          
	afx_msg void On01();                                            
};                                                                  
IMPLEMENT_DYNCREATE(CMyView, CView)                       
BEGIN_MESSAGE_MAP(CMyView, CView)                                                     
	ON_COMMAND(ID01, On01)                                           
END_MESSAGE_MAP()                                                    
                                                                    
/*应用程序类*/                                                      
class CApp : public CWinApp{	virtual BOOL InitInstance(); };
BOOL CApp::InitInstance ()                             
{		CSingleDocTemplate* pDocTemplate= new CSingleDocTemplate(IDR_MENU1,RUNTIME_CLASS(CDoc),
													

RUNTIME_CLASS(CMainFrame),RUNTIME_CLASS(CMyView));
		AddDocTemplate(pDocTemplate);CCommandLineInfo cmdInfo;ParseCommandLine(cmdInfo);
		if (!ProcessShellCommand(cmdInfo))	return FALSE;            
		m_pMainWnd->ShowWindow(SW_SHOW);m_pMainWnd->UpdateWindow();  
		m_pMainWnd->SetWindowPos(NULL,0,0,800,600,0);                
		m_pMainWnd->SetWindowText("版本1.01.");
		return TRUE;                                                 
}                                                                    
CApp theApp;             
                                
//区域三：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓
void CMyView::On01() 
{

	CBitmap hbitmap;         //利用"位图类"声明一个标识符为"hbitmap"的变量
	BITMAP bm;               //利用"位图文件头结构体"声明一个标识符为"bm"的变量
    unsigned char *buffer;            //声明了一个8位整型数"buffer"指针
	int Hang,Lie,WidthBytes;       //声明行总数目，列总数目，单个象素占据的字节个数

	hbitmap.DeleteObject();                                  //位图类的清空位图函数
	hbitmap.LoadBitmap (IDB_BITMAP2);                        //加载位图文件
	hbitmap.GetBitmap (&bm);     //将所加载的位图文件的头文件信息装载到标识符为"bm"的变量中
	Hang=bm.bmHeight,Lie=bm.bmWidth,WidthBytes=bm.bmWidthBytes;
	buffer= new unsigned char[Hang*WidthBytes];    
	//新开辟一段由上述三个变量决定个数的8位整形数组然后被buffer指针所指向
	hbitmap.GetBitmapBits (Hang*WidthBytes, buffer);   //录入buffer数组的位图数据

    //buffer[j*WidthBytes+i*4+k];

	int i,j;
	int tempR,tempG,tempB;

    CDC *pDC;
	pDC=GetDC();


    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));
		}
	}

	MessageBox("以下将RGB图转灰度图");
	int temp;
    unsigned char *buffer2,*buffer3;            //声明了一个8位整型数"buffer"指针
	buffer2= new unsigned char[Hang*Lie];	   //存储灰度图字节数据
	buffer3= new unsigned char[Hang*Lie];	   //存储线性点运算处理后的灰度级数据
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
			temp=(tempR+tempG+tempB)/3;
    	    pDC->SetPixel(i,j,RGB(temp,temp,temp));
			buffer2[j*Lie+i]=temp;
		}
	}
    
    //结束以上流程后，我们最终得到了存储灰度图数据的buffer2

	MessageBox("以下灰度图的点运算输出");

	
    //以下为线性点运算
    float a=1.5,b=0;	//解释一下这个点运算的作用，b=0对整体的图像没有任何影响
    				   //a>1，即整体图像的对比度增加，亮的地方越亮，暗的地方越暗
	int DA,DB;		   //DA临时存储的是灰度值，DB存储的是计算之后的结果
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			DA=buffer2[j*Lie+i];	//将循环到的像素的灰度值存入DA
			DB=a*DA+b;			   //进行简单的计算工作，对读取到的灰度值进行点运算
			if(DB>255) DB=255;	    //一般是不可能大于255的，因为这个灰度值是从前面的RGB图像求平均转化而来的，
            					   //不过为了程序的严谨还是设定将超过255的灰度值设定为255
			if(DB<0) DB=0;		   //同上面所述也是为了严谨，将小于0的灰度值转化为
			buffer3[j*Lie+i]=DB;    //存储处理后的灰度级
			
			temp=buffer3[j*Lie+i];	//这一步太太冗余了，没必要，使用DB直接代替temp它不香吗
			pDC->SetPixel(i,j,RGB(temp,temp,temp));	//将数据加载到位图
		}
	}
/*
	 //以下为非线性点运算----轮廓线
	int DA,DB;
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			DA=buffer2[j*Lie+i];
			
			if(DA<128)  DB=0;
			if(DA>=128&&DA<=128+5) DB=255;
			if(DA>128+5) DB=0;

			if(DB>255) DB=255;
			if(DB<0) DB=0;
			buffer3[j*Lie+i]=DB;
			
			temp=buffer3[j*Lie+i];
			pDC->SetPixel(i,j,RGB(temp,temp,temp));
		}
	}*/

}

~~~

### 04 代数运算--加法（生成叠加图像）

~~~c++
#include <afxwin.h> 
#include <afxext.h> 
#include <afxdisp.h>   
#include <afxdtctl.h>
#include "resource.h"

/*文档类*/
class CDoc: public CDocument
{	
	DECLARE_DYNCREATE(CDoc)
};
IMPLEMENT_DYNCREATE(CDoc, CDocument)
/*主帧窗口类*/
class CMainFrame : public CFrameWnd
{	
	DECLARE_DYNCREATE(CMainFrame)
};
IMPLEMENT_DYNCREATE(CMainFrame, CFrameWnd)

/*视图类*/
class CMyView : public CView
{
public:
	//区域一：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓

	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	DECLARE_MESSAGE_MAP()
	DECLARE_DYNCREATE(CMyView)
public:
	CMyView()
	{
	//区域二：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓


	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	}
	virtual ~CMyView(){}
	public:
	virtual void OnDraw(CDC* pDC){} 
	protected:                                                          
	afx_msg void On01();                                            
};                                                                  
IMPLEMENT_DYNCREATE(CMyView, CView)                       
BEGIN_MESSAGE_MAP(CMyView, CView)                                                    
	ON_COMMAND(ID01, On01)                                           
END_MESSAGE_MAP()                                                    
                                                                    
/*应用程序类*/                                                      
class CApp : public CWinApp{	virtual BOOL InitInstance(); };
BOOL CApp::InitInstance ()                             
{		CSingleDocTemplate* pDocTemplate= new CSingleDocTemplate(IDR_MENU1,RUNTIME_CLASS(CDoc),
													

RUNTIME_CLASS(CMainFrame),RUNTIME_CLASS(CMyView));
		AddDocTemplate(pDocTemplate);CCommandLineInfo cmdInfo;ParseCommandLine(cmdInfo);
		if (!ProcessShellCommand(cmdInfo))	return FALSE;            
		m_pMainWnd->ShowWindow(SW_SHOW);m_pMainWnd->UpdateWindow();  
		m_pMainWnd->SetWindowPos(NULL,0,0,800,600,0);                
		m_pMainWnd->SetWindowText("版本1.01.");
		return TRUE;                                                 
}                                                                    
CApp theApp;             

#include "math.h"                                
//区域三：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓
void CMyView::On01() 
{

	CBitmap hbitmap;         //利用"位图类"声明一个标识符为"hbitmap"的变量
	BITMAP bm;               //利用"位图文件头结构体"声明一个标识符为"bm"的变量
    unsigned char *buffer;            //声明了一个8位整型数"buffer"指针
	int Hang,Lie,WidthBytes;       //声明行总数目，列总数目，单个象素占据的字节个数

	hbitmap.DeleteObject();                                  //位图类的清空位图函数
	hbitmap.LoadBitmap (IDB_BITMAP2);                        //加载位图文件
	hbitmap.GetBitmap (&bm);     //将所加载的位图文件的头文件信息装载到标识符为"bm"的变量中
	Hang=bm.bmHeight,Lie=bm.bmWidth,WidthBytes=bm.bmWidthBytes;
	buffer= new unsigned char[Hang*WidthBytes];    
	//新开辟一段由上述三个变量决定个数的8位整形数组然后被buffer指针所指向
	hbitmap.GetBitmapBits (Hang*WidthBytes, buffer);   //录入buffer数组的位图数据

	CBitmap hbitmap2;         //利用"位图类"声明一个标识符为"hbitmap"的变量
	BITMAP bm2;               //利用"位图文件头结构体"声明一个标识符为"bm"的变量
    unsigned char *buffer2,*buffer3;            //声明了一个8位整型数"buffer"指针

	hbitmap2.DeleteObject();                                  //位图类的清空位图函数
	hbitmap2.LoadBitmap (IDB_BITMAP3);                        //加载位图文件
	hbitmap2.GetBitmap (&bm2);     //将所加载的位图文件的头文件信息装载到标识符为"bm"的变量中
	buffer2= new unsigned char[Hang*WidthBytes];    
	//新开辟一段由上述三个变量决定个数的8位整形数组然后被buffer指针所指向
	hbitmap2.GetBitmapBits (Hang*WidthBytes, buffer2);   //录入buffer数组的位图数据

	buffer3= new unsigned char[Hang*WidthBytes];    


	int i,j;
	int tempR,tempG,tempB;
	int tempR1,tempG1,tempB1;
	int tempR2,tempG2,tempB2;
	int tempR3,tempG3,tempB3;

    CDC *pDC;
	pDC=GetDC();

    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));
		}
	}

    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer2[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer2[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer2[j*WidthBytes+i*WidthBytes/Lie+0];
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));
		}
	}
	
    //以上代码只是完成了一个功能
    /*
    	将图像的RGB数据读出来，按顺序放在两个数组中（buffer和buffer2）
    	其他的就是转化位图操作而已
    */
	//以下进行代数运算--加法(生成叠加图像)
	MessageBox("以下进行代数运算--加法(生成叠加图像)");
	
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{	
            /**
            *	以下代码只是分别将两个数据数组的对应相同位置的像素读取出来，然后求平均
            *	最后再将数据按顺序存储到buffer3指定的数组中
            */
            
			tempR1=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG1=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB1=buffer[j*WidthBytes+i*WidthBytes/Lie+0];

			tempR2=buffer2[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG2=buffer2[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB2=buffer2[j*WidthBytes+i*WidthBytes/Lie+0];

			tempR3=(tempR1+tempR2)/2;
			tempG3=(tempG1+tempG2)/2;
			tempB3=(tempB1+tempB2)/2;


			buffer3[j*WidthBytes+i*WidthBytes/Lie+2]=tempR3;
			buffer3[j*WidthBytes+i*WidthBytes/Lie+1]=tempG3;
			buffer3[j*WidthBytes+i*WidthBytes/Lie+0]=tempB3;
			pDC->SetPixel(i,j,RGB(tempR3,tempG3,tempB3));
		}
	}	//至此，原图的灰度图生成，放在buffer2内存条中   

}

~~~



### 05 代数运算--减法（梯度幅值）

> 这个代码的思想需要重点学习下

```c++
/*
	这个程序的写的思想很重要，重点学习
*/
#include <afxwin.h> 
#include <afxext.h> 
#include <afxdisp.h>   
#include <afxdtctl.h>
#include "resource.h"

/*文档类*/
class CDoc: public CDocument
{	
	DECLARE_DYNCREATE(CDoc)
};
IMPLEMENT_DYNCREATE(CDoc, CDocument)
/*主帧窗口类*/
class CMainFrame : public CFrameWnd
{	
	DECLARE_DYNCREATE(CMainFrame)
};
IMPLEMENT_DYNCREATE(CMainFrame, CFrameWnd)

/*视图类*/
class CMyView : public CView
{
public:
	//区域一：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓

	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	DECLARE_MESSAGE_MAP()
	DECLARE_DYNCREATE(CMyView)
public:
	CMyView()
	{
	//区域二：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓


	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	}
	virtual ~CMyView(){}
	public:
	virtual void OnDraw(CDC* pDC){} 
	protected:                                                          
	afx_msg void On01();                                            
};                                                                  
IMPLEMENT_DYNCREATE(CMyView, CView)                       
BEGIN_MESSAGE_MAP(CMyView, CView)                                                    
	ON_COMMAND(ID01, On01)                                           
END_MESSAGE_MAP()                                                    
                                                                    
/*应用程序类*/                                                      
class CApp : public CWinApp{	virtual BOOL InitInstance(); };
BOOL CApp::InitInstance ()                             
{		CSingleDocTemplate* pDocTemplate= new CSingleDocTemplate(IDR_MENU1,RUNTIME_CLASS(CDoc),
													

RUNTIME_CLASS(CMainFrame),RUNTIME_CLASS(CMyView));
		AddDocTemplate(pDocTemplate);CCommandLineInfo cmdInfo;ParseCommandLine(cmdInfo);
		if (!ProcessShellCommand(cmdInfo))	return FALSE;            
		m_pMainWnd->ShowWindow(SW_SHOW);m_pMainWnd->UpdateWindow();  
		m_pMainWnd->SetWindowPos(NULL,0,0,800,600,0);                
		m_pMainWnd->SetWindowText("版本1.01.");
		return TRUE;                                                 
}                                                                    
CApp theApp;             

#include "math.h"                                
//区域三：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓
void CMyView::On01() 
{

	CBitmap hbitmap;         //利用"位图类"声明一个标识符为"hbitmap"的变量
	BITMAP bm;               //利用"位图文件头结构体"声明一个标识符为"bm"的变量
    unsigned char *buffer;            //声明了一个8位整型数"buffer"指针
	int Hang,Lie,WidthBytes;       //声明行总数目，列总数目，单个象素占据的字节个数

	hbitmap.DeleteObject();                                  //位图类的清空位图函数
	hbitmap.LoadBitmap (IDB_BITMAP2);                        //加载位图文件
	hbitmap.GetBitmap (&bm);     //将所加载的位图文件的头文件信息装载到标识符为"bm"的变量中
	Hang=bm.bmHeight,Lie=bm.bmWidth,WidthBytes=bm.bmWidthBytes;
	buffer= new unsigned char[Hang*WidthBytes];    
	//新开辟一段由上述三个变量决定个数的8位整形数组然后被buffer指针所指向
	hbitmap.GetBitmapBits (Hang*WidthBytes, buffer);   //录入buffer数组的位图数据

    //buffer[j*WidthBytes+i*4+k];

	int i,j;
	int tempR,tempG,tempB;

    CDC *pDC;
	pDC=GetDC();


    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));
		}
	}

//以下进行图像的减法运算(求梯度幅度)
	MessageBox("以下进行图像的减法运算(求梯度幅度)");
	BYTE *buffer2= new unsigned char[Hang*Lie];    
	BYTE *buffer3= new unsigned char[Hang*Lie];    
	BYTE *buffer4= new unsigned char[Hang*Lie];   
	BYTE *buffer5= new unsigned char[Hang*Lie];   
	
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];

			buffer2[j*Lie+i*1+0]=(tempR+tempG+tempB)/3;
			pDC->SetPixel(i,j,RGB(buffer2[j*Lie+i*1+0],buffer2[j*Lie+i*1+0],buffer2[j*Lie+i*1+0]));
		}
	}//至此，原图的灰度图生成，放在buffer2内存条中


    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie-1;i++)
		{
			buffer3[j*Lie+i*1]=buffer2[j*Lie+(i+1)*1];
		}
	}//至此，原图灰度图的左移一个像素点的图像生成，放在buffer3内存条中

    for(j=0;j<Hang-1;j++)
	{
		for(i=0;i<Lie;i++)
		{
			buffer4[j*Lie+i*1]=buffer2[(j+1)*Lie+i*1];
		}
	}//至此，原图灰度图的上移一个像素点的图像生成，放在buffer4内存条中

    /*
    	
    */
    
	MessageBox("两个平移图构造完毕");
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
		    BYTE temp1,temp2,temp3,temp4,temp5;
			temp1=buffer2[j*Lie+i*1];
			temp2=buffer3[j*Lie+i*1];
			temp3=buffer4[j*Lie+i*1];
			temp4=fabs(temp2-temp1);
			temp5=fabs(temp3-temp1);
			if(temp5>temp4)
				buffer5[j*Lie+i*1]=temp5;
			else
				buffer5[j*Lie+i*1]=temp4;
			
			int temp6;
			temp6=buffer5[j*Lie+i*1]*10;
			if(temp6>255)
				buffer5[j*Lie+i*1]=255;
			else
				buffer5[j*Lie+i*1]=temp6;
			pDC->SetPixel(i,j,RGB(buffer5[j*Lie+i*1+0],buffer5[j*Lie+i*1+0],buffer5[j*Lie+i*1+0]));
		}
	}//至此，原图灰度图的梯度幅度图生成，放在buffer5内存条中

}
```

### 06 几何运算

~~~C++
#include <afxwin.h> 
#include <afxext.h> 
#include <afxdisp.h>   
#include <afxdtctl.h>
#include "resource.h"

/*文档类*/
class CDoc: public CDocument
{	
	DECLARE_DYNCREATE(CDoc)
};
IMPLEMENT_DYNCREATE(CDoc, CDocument)
/*主帧窗口类*/
class CMainFrame : public CFrameWnd
{	
	DECLARE_DYNCREATE(CMainFrame)
};
IMPLEMENT_DYNCREATE(CMainFrame, CFrameWnd)

/*视图类*/
class CMyView : public CView
{
public:
	//区域一：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓

	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	DECLARE_MESSAGE_MAP()
	DECLARE_DYNCREATE(CMyView)
public:
	CMyView()
	{
	//区域二：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓


	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	}
	virtual ~CMyView(){}
	public:
	virtual void OnDraw(CDC* pDC){} 
	protected:                                                          
	afx_msg void On01();                                            
};                                                                  
IMPLEMENT_DYNCREATE(CMyView, CView)                       
BEGIN_MESSAGE_MAP(CMyView, CView)                                                     
	ON_COMMAND(ID01, On01)                                           
END_MESSAGE_MAP()                                                    
                                                                    
/*应用程序类*/                                                      
class CApp : public CWinApp{	virtual BOOL InitInstance(); };
BOOL CApp::InitInstance ()                             
{		CSingleDocTemplate* pDocTemplate= new CSingleDocTemplate(IDR_MENU1,RUNTIME_CLASS(CDoc),
													

RUNTIME_CLASS(CMainFrame),RUNTIME_CLASS(CMyView));
		AddDocTemplate(pDocTemplate);CCommandLineInfo cmdInfo;ParseCommandLine(cmdInfo);
		if (!ProcessShellCommand(cmdInfo))	return FALSE;            
		m_pMainWnd->ShowWindow(SW_SHOW);m_pMainWnd->UpdateWindow();  
		m_pMainWnd->SetWindowPos(NULL,0,0,800,600,0);                
		m_pMainWnd->SetWindowText("版本1.01.");
		return TRUE;                                                 
}                                                                    
CApp theApp;             
                                
//区域三：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓
#include "math.h"
void CMyView::On01() 
{

	CBitmap hbitmap;         //利用"位图类"声明一个标识符为"hbitmap"的变量
	BITMAP bm;               //利用"位图文件头结构体"声明一个标识符为"bm"的变量
    unsigned char *buffer;            //声明了一个8位整型数"buffer"指针
	int Hang,Lie,WidthBytes;       //声明行总数目，列总数目，单个象素占据的字节个数

	hbitmap.DeleteObject();                                  //位图类的清空位图函数
	hbitmap.LoadBitmap (IDB_BITMAP2);                        //加载位图文件
	hbitmap.GetBitmap (&bm);     //将所加载的位图文件的头文件信息装载到标识符为"bm"的变量中
	Hang=bm.bmHeight,Lie=bm.bmWidth,WidthBytes=bm.bmWidthBytes;
	buffer= new unsigned char[Hang*WidthBytes];    
	//新开辟一段由上述三个变量决定个数的8位整形数组然后被buffer指针所指向
	hbitmap.GetBitmapBits (Hang*WidthBytes, buffer);   //录入buffer数组的位图数据

    //buffer[j*WidthBytes+i*4+k];

	int i,j;
	int tempR,tempG,tempB;

    CDC *pDC;
	pDC=GetDC();


    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));
		}
	}

	MessageBox("以下将RGB图转灰度图");
	int temp;
    unsigned char *buffer2,*buffer3;            //声明了一个8位整型数"buffer"指针
	buffer2= new unsigned char[Hang*Lie];
	buffer3= new unsigned char[Hang*Lie];
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
			temp=(tempR+tempG+tempB)/3;
    	    pDC->SetPixel(i,j,RGB(temp,temp,temp));
			buffer2[j*Lie+i]=temp;
		}
	}
  
	MessageBox("以下灰度图的几何运算输出");

    Invalidate();
	UpdateWindow();

    double a=200,b=100,Beta=3.14/6;
	double T11=cos(Beta),T12=sin(Beta),T13=-a*cos(Beta)-b*sin(Beta)+a;
	double T21=-sin(Beta),T22=cos(Beta),T23=a*sin(Beta)-b*cos(Beta)+b;
	double T31=0,T32=0,T33=1;

    /*
    double T11=1.25,T12=0,T13=0;
	double T21=0,T22=1.25,T23=0;
	double T31=0,T32=0,T33=1;
	*/

	int x0=30,y0=40;

	int x1,y1;

	/*	
	x1=T11*x0+T12*y0+T13*1;
	y1=T21*x0+T22*y0+T23*1;

	CString str;
	str.Format("x1=%d,y1=%d",x1,y1);
	pDC->TextOut(0,0,str);
	*/

	for(y0=0;y0<Hang;y0++)
	{
		for(x0=0;x0<Lie;x0++)
		{
			x1=T11*x0+T12*y0+T13*1;
			y1=T21*x0+T22*y0+T23*1;
			temp=buffer2[y0*Lie+x0];
    	    pDC->SetPixel(x1,y1,RGB(temp,temp,temp));
		}
	}

}

~~~



### 07 几何运算加插值运算

~~~C++
#include <afxwin.h> 
#include <afxext.h> 
#include <afxdisp.h>   
#include <afxdtctl.h>
#include "resource.h"

/*文档类*/
class CDoc: public CDocument
{	
	DECLARE_DYNCREATE(CDoc)
};
IMPLEMENT_DYNCREATE(CDoc, CDocument)
/*主帧窗口类*/
class CMainFrame : public CFrameWnd
{	
	DECLARE_DYNCREATE(CMainFrame)
};
IMPLEMENT_DYNCREATE(CMainFrame, CFrameWnd)

/*视图类*/
class CMyView : public CView
{
public:
	//区域一：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓

	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	DECLARE_MESSAGE_MAP()
	DECLARE_DYNCREATE(CMyView)
public:
	CMyView()
	{
	//区域二：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓


	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	}
	virtual ~CMyView(){}
	public:
	virtual void OnDraw(CDC* pDC){} 
	protected:                                                          
	afx_msg void On01();                                            
};                                                                  
IMPLEMENT_DYNCREATE(CMyView, CView)                       
BEGIN_MESSAGE_MAP(CMyView, CView)                                                    
	ON_COMMAND(ID01, On01)                                           
END_MESSAGE_MAP()                                                    
                                                                    
/*应用程序类*/                                                      
class CApp : public CWinApp{	virtual BOOL InitInstance(); };
BOOL CApp::InitInstance ()                             
{		CSingleDocTemplate* pDocTemplate= new CSingleDocTemplate(IDR_MENU1,RUNTIME_CLASS(CDoc),
													

RUNTIME_CLASS(CMainFrame),RUNTIME_CLASS(CMyView));
		AddDocTemplate(pDocTemplate);CCommandLineInfo cmdInfo;ParseCommandLine(cmdInfo);
		if (!ProcessShellCommand(cmdInfo))	return FALSE;            
		m_pMainWnd->ShowWindow(SW_SHOW);m_pMainWnd->UpdateWindow();  
		m_pMainWnd->SetWindowPos(NULL,0,0,800,600,0);                
		m_pMainWnd->SetWindowText("版本1.01.");
		return TRUE;                                                 
}                                                                    
CApp theApp;             
                                
//区域三：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓
#include "math.h"
void CMyView::On01() 
{

	CBitmap hbitmap;         //利用"位图类"声明一个标识符为"hbitmap"的变量
	BITMAP bm;               //利用"位图文件头结构体"声明一个标识符为"bm"的变量
    unsigned char *buffer;            //声明了一个8位整型数"buffer"指针
	int Hang,Lie,WidthBytes;       //声明行总数目，列总数目，单个象素占据的字节个数

	hbitmap.DeleteObject();                                  //位图类的清空位图函数
	hbitmap.LoadBitmap (IDB_BITMAP2);                        //加载位图文件
	hbitmap.GetBitmap (&bm);     //将所加载的位图文件的头文件信息装载到标识符为"bm"的变量中
	Hang=bm.bmHeight,Lie=bm.bmWidth,WidthBytes=bm.bmWidthBytes;
	buffer= new unsigned char[Hang*WidthBytes];    
	//新开辟一段由上述三个变量决定个数的8位整形数组然后被buffer指针所指向
	hbitmap.GetBitmapBits (Hang*WidthBytes, buffer);   //录入buffer数组的位图数据

    //buffer[j*WidthBytes+i*4+k];

	int i,j;
	int tempR,tempG,tempB;

    CDC *pDC;
	pDC=GetDC();


    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));
		}
	}

	MessageBox("以下将RGB图转灰度图");
	int temp;
    unsigned char *buffer2,*buffer3;            //声明了一个8位整型数"buffer"指针
	buffer2= new unsigned char[Hang*Lie];
	buffer3= new unsigned char[Hang*Lie];
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
			temp=(tempR+tempG+tempB)/3;
    	    pDC->SetPixel(i,j,RGB(temp,temp,temp));
			buffer2[j*Lie+i]=temp;
		}
	}
  
	MessageBox("以下灰度图的几何运算输出");

    Invalidate();
	UpdateWindow();

    double a=200,b=100,Beta=3.14/6;
	double T11=cos(Beta),T12=sin(Beta),T13=-a*cos(Beta)-b*sin(Beta)+a;
	double T21=-sin(Beta),T22=cos(Beta),T23=a*sin(Beta)-b*cos(Beta)+b;
	double T31=0,T32=0,T33=1;

	/*
	double T11=1.25,T12=0,T13=0;
	double T21=0,T22=1.25,T23=0;
	double T31=0,T32=0,T33=1;
	*/

	int x0=30,y0=40;

	int x1,y1;

	/*	
	x1=T11*x0+T12*y0+T13*1;
	y1=T21*x0+T22*y0+T23*1;

	CString str;
	str.Format("x1=%d,y1=%d",x1,y1);
	pDC->TextOut(0,0,str);
	*/


	for(int k=0;k<Hang*Lie;k++)  //旋转后的图寄存在buffer3内存中，并且先清零
		buffer3[k]=0;

	for(y0=0;y0<Hang;y0++)
	{
		for(x0=0;x0<Lie;x0++)
		{
			x1=T11*x0+T12*y0+T13*1;
			y1=T21*x0+T22*y0+T23*1;
			temp=buffer2[y0*Lie+x0];
    	    pDC->SetPixel(x1,y1,RGB(temp,temp,temp));

			if(x1>Lie-1) x1=Lie-1;
			if(x1<0) x1=0;
			if(y1>Hang-1) y1=Hang-1;
			if(y1<0) y1=0;
			buffer3[y1*Lie+x1]=temp;
		}
	}


	MessageBox("以下进行插值运算");
	double Tni11=0.8660,Tni12=-0.5000,Tni13=76.7949;
    double Tni21=0.5000,Tni22=0.8660,Tni23=-86.6025;
    double Tni31=0,Tni32=0,Tni33=1.0000;

	double x0pi,y0pi;
	for(y1=0;y1<Hang;y1++)
	{
		for(x1=0;x1<Lie;x1++)
		{
			if(buffer3[y1*Lie+x1]==0)
			{
				x0pi=Tni11*x1+Tni12*y1+Tni13;
				y0pi=Tni21*x1+Tni22*y1+Tni23;

				if(x0pi-(int)x0pi>0.5) x0pi=(int)x0pi+1;
				else x0pi=(int)x0pi;
				if(y0pi-(int)y0pi>0.5) y0pi=(int)y0pi+1;
				else y0pi=(int)y0pi;
			
				if(x0pi>Lie-1) continue;
				if(x0pi<0) continue;
				if(y0pi>Hang-1) continue;
				if(y0pi<0) continue;				
				buffer3[y1*Lie+x1]=buffer2[(int)y0pi*Lie+(int)x0pi];
			}
		}
	}

	//最后将插值的效果显示
    Invalidate();
	UpdateWindow();
	for(y1=0;y1<Hang;y1++)
	{
		for(x1=0;x1<Lie;x1++)
		{
    	    pDC->SetPixel(x1,y1,RGB(buffer3[y1*Lie+x1],buffer3[y1*Lie+x1],buffer3[y1*Lie+x1]));
		}
	}

}



~~~



### 08 直方图均衡化

~~~C++
#include <afxwin.h> 
#include <afxext.h> 
#include <afxdisp.h>   
#include <afxdtctl.h>
#include "resource.h"

/*文档类*/
class CDoc: public CDocument
{	
	DECLARE_DYNCREATE(CDoc)
};
IMPLEMENT_DYNCREATE(CDoc, CDocument)
/*主帧窗口类*/
class CMainFrame : public CFrameWnd
{	
	DECLARE_DYNCREATE(CMainFrame)
};
IMPLEMENT_DYNCREATE(CMainFrame, CFrameWnd)

/*视图类*/
class CMyView : public CView
{
public:
	//区域一：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓

	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	DECLARE_MESSAGE_MAP()
	DECLARE_DYNCREATE(CMyView)
public:
	CMyView()
	{
	//区域二：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓


	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	}
	virtual ~CMyView(){}
	public:
	virtual void OnDraw(CDC* pDC){} 
	protected:                                                          
	afx_msg void On01();                                            
};                                                                  
IMPLEMENT_DYNCREATE(CMyView, CView)                       
BEGIN_MESSAGE_MAP(CMyView, CView)                                                    
	ON_COMMAND(ID01, On01)                                           
END_MESSAGE_MAP()                                                    
                                                                    
/*应用程序类*/                                                      
class CApp : public CWinApp{	virtual BOOL InitInstance(); };
BOOL CApp::InitInstance ()                             
{		CSingleDocTemplate* pDocTemplate= new CSingleDocTemplate(IDR_MENU1,RUNTIME_CLASS(CDoc),
													

RUNTIME_CLASS(CMainFrame),RUNTIME_CLASS(CMyView));
		AddDocTemplate(pDocTemplate);CCommandLineInfo cmdInfo;ParseCommandLine(cmdInfo);
		if (!ProcessShellCommand(cmdInfo))	return FALSE;            
		m_pMainWnd->ShowWindow(SW_SHOW);m_pMainWnd->UpdateWindow();  
		m_pMainWnd->SetWindowPos(NULL,0,0,800,600,0);                
		m_pMainWnd->SetWindowText("版本1.01.");
		return TRUE;                                                 
}                                                                    
CApp theApp;             
                                
//区域三：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓
void CMyView::On01() 
{

	CBitmap hbitmap;         //利用"位图类"声明一个标识符为"hbitmap"的变量
	BITMAP bm;               //利用"位图文件头结构体"声明一个标识符为"bm"的变量
    unsigned char *buffer;            //声明了一个8位整型数"buffer"指针
	int Hang,Lie,WidthBytes;       //声明行总数目，列总数目，单个象素占据的字节个数

	hbitmap.DeleteObject();                                  //位图类的清空位图函数
	hbitmap.LoadBitmap (IDB_BITMAP2);                        //加载位图文件
	hbitmap.GetBitmap (&bm);     //将所加载的位图文件的头文件信息装载到标识符为"bm"的变量中
	Hang=bm.bmHeight,Lie=bm.bmWidth,WidthBytes=bm.bmWidthBytes;
	buffer= new unsigned char[Hang*WidthBytes];    
	//新开辟一段由上述三个变量决定个数的8位整形数组然后被buffer指针所指向
	hbitmap.GetBitmapBits (Hang*WidthBytes, buffer);   //录入buffer数组的位图数据

    //buffer[j*WidthBytes+i*4+k];

	int i,j;
	int tempR,tempG,tempB;

    CDC *pDC;
	pDC=GetDC();


    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));
		}
	}

	MessageBox("以下将RGB图转灰度图");
	int temp;
    unsigned char *buffer2;            //声明了一个8位整型数"buffer"指针
	buffer2= new unsigned char[Hang*Lie];
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
			temp=(tempR+tempG+tempB)/3;
    	    pDC->SetPixel(i,j,RGB(temp,temp,temp));
			buffer2[j*Lie+i]=temp;
		}
	}

	int Zhifangtu[256];
    for(int k=0;k<256;k++)
		Zhifangtu[k]=0;
	MessageBox("以下灰度图的直方图输出");
	for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			temp=buffer2[j*Lie+i];
			Zhifangtu[temp]++;
		}
	}

	FILE* fp=fopen("F://abc.txt","w");
	for(k=0;k<256;k++)
		fprintf(fp,"灰阶＝%d 次数=%d\n",k,Zhifangtu[k]);
	fclose(fp);

	for(k=0;k<256;k++)
	{
		pDC->MoveTo(Lie+20+k,Hang);
		pDC->LineTo(Lie+20+k,Hang-Zhifangtu[k]*0.1);
	}

	MessageBox("以下进行直方图均衡化");
	double Prk[256];
	for(k=0;k<256;k++)
		Prk[k]=(double)Zhifangtu[k]/(Hang*Lie);   //计算输入图像的归一化直方图函数
	double Sk[256];
	for(k=0;k<256;k++)
	{
		Sk[k]=0.0;
		for(int k2=0;k2<=k;k2++)
		   Sk[k]+=Prk[k2];       //完成直方图均衡化公式中的累加运算
	}
	double S[256];
	for(k=0;k<256;k++)
	{
		S[k]=Sk[k]*255;   //S[k]就是直方图均衡化的映射函数（即点运算的灰度变换函数）
		if(S[k]-(int)S[k]>0.5) S[k]=(int)S[k]+1;  //完成映射函数的四舍五入取整
		else S[k]=(int)S[k];
	}

	int T[256];
	for(int r=0;r<256;r++)
		T[r]=(int)S[r];    //T[r]就是均衡化映射后的新灰度值


	for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			r=buffer2[j*Lie+i];   //将输入图像j行i列的像素值赋予r
    	    pDC->SetPixel(i,j,RGB(T[r],T[r],T[r]));  //T[r]是映射后的新灰度值，即输出图像在j行i列
		}
	}
}



~~~



### 09 空域滤波（模板卷积运算）

~~~C++
#include <afxwin.h> 
#include <afxext.h> 
#include <afxdisp.h>   
#include <afxdtctl.h>
#include "resource.h"

/*文档类*/
class CDoc: public CDocument
{	
	DECLARE_DYNCREATE(CDoc)
};
IMPLEMENT_DYNCREATE(CDoc, CDocument)
/*主帧窗口类*/
class CMainFrame : public CFrameWnd
{	
	DECLARE_DYNCREATE(CMainFrame)
};
IMPLEMENT_DYNCREATE(CMainFrame, CFrameWnd)

/*视图类*/
class CMyView : public CView
{
public:
	//区域一：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓

	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	DECLARE_MESSAGE_MAP()
	DECLARE_DYNCREATE(CMyView)
public:
	CMyView()
	{
	//区域二：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓


	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	}
	virtual ~CMyView(){}
	public:
	virtual void OnDraw(CDC* pDC){} 
	protected:                                                          
	afx_msg void On01();                                            
};                                                                  
IMPLEMENT_DYNCREATE(CMyView, CView)                       
BEGIN_MESSAGE_MAP(CMyView, CView)                                                    
	ON_COMMAND(ID01, On01)                                           
END_MESSAGE_MAP()                                                    
                                                                    
/*应用程序类*/                                                      
class CApp : public CWinApp{	virtual BOOL InitInstance(); };
BOOL CApp::InitInstance ()                             
{		CSingleDocTemplate* pDocTemplate= new CSingleDocTemplate(IDR_MENU1,RUNTIME_CLASS(CDoc),
													

RUNTIME_CLASS(CMainFrame),RUNTIME_CLASS(CMyView));
		AddDocTemplate(pDocTemplate);CCommandLineInfo cmdInfo;ParseCommandLine(cmdInfo);
		if (!ProcessShellCommand(cmdInfo))	return FALSE;            
		m_pMainWnd->ShowWindow(SW_SHOW);m_pMainWnd->UpdateWindow();  
		m_pMainWnd->SetWindowPos(NULL,0,0,800,600,0);                
		m_pMainWnd->SetWindowText("版本1.01.");
		return TRUE;                                                 
}                                                                    
CApp theApp;             
                                
//区域三：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓
void CMyView::On01() 
{

	CBitmap hbitmap;         //利用"位图类"声明一个标识符为"hbitmap"的变量
	BITMAP bm;               //利用"位图文件头结构体"声明一个标识符为"bm"的变量
    unsigned char *buffer;            //声明了一个8位整型数"buffer"指针
	int Hang,Lie,WidthBytes;       //声明行总数目，列总数目，单个象素占据的字节个数

	hbitmap.DeleteObject();                                  //位图类的清空位图函数
	hbitmap.LoadBitmap (IDB_BITMAP2);                        //加载位图文件
	hbitmap.GetBitmap (&bm);     //将所加载的位图文件的头文件信息装载到标识符为"bm"的变量中
	Hang=bm.bmHeight,Lie=bm.bmWidth,WidthBytes=bm.bmWidthBytes;
	buffer= new unsigned char[Hang*WidthBytes];    
	//新开辟一段由上述三个变量决定个数的8位整形数组然后被buffer指针所指向
	hbitmap.GetBitmapBits (Hang*WidthBytes, buffer);   //录入buffer数组的位图数据

    //buffer[j*WidthBytes+i*4+k];

	int i,j;
	int tempR,tempG,tempB;

    CDC *pDC;
	pDC=GetDC();


    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));
		}
	}

	MessageBox("以下将RGB图转灰度图");
	int temp;
    unsigned char *buffer2,*buffer3;            //声明了一个8位整型数"buffer"指针
	buffer2= new unsigned char[Hang*Lie];
	buffer3= new unsigned char[Hang*Lie];
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
			temp=(tempR+tempG+tempB)/3;
    	    pDC->SetPixel(i,j,RGB(temp,temp,temp));
			buffer2[j*Lie+i]=temp;
		}
	}

	MessageBox("以下进行空域滤波(模板卷积运算)");
    double h1=0.0,h2=-4.0,h3=0.0,h4=-4.0,h5=16.0,h6=-4.0,h7=0.0,h8=-4.0,h9=0.0;//拉普拉斯算子
    //double h1=-4.0,h2=-4.0,h3=8.0,h4=-4.0,h5=8.0,h6=-4.0,h7=8.0,h8=-4.0,h9=-4.0;//定向滤波
    //double h11=0,h12=0,h13=0,h14=0,h17=0,h15=4,h16=4,h18=-4,h19=-4;//罗伯特算子
    //double h21=0,h22=0,h23=0,h24=0,h27=0,h25=-4,h26=4,h28=-4,h29=4;
    //double h31=0,h32=0,h33=0,h34=0,h37=0,h35=4,h36=-4,h38=4,h39=-4;
    //double h41=0,h42=0,h43=0,h44=0,h47=0,h45=-4,h46=-4,h48=4,h49=4;
    //double h1=1.0/9,h2=1.0/9,h3=1.0/9,h4=1.0/9,h5=1.0/9,h6=1.0/9,h7=1.0/9,h8=1.0/9,h9=1.0/9;//邻域平均算子
    double f1,f2,f3,f4,f5,f6,f7,f8,f9;
	double temp1,temp2,temp3,temp4;
	double temp5;
	int Yuzhi;
	for(j=1;j<Hang-1;j++)
	{
		for(i=1;i<Lie-1;i++)
		{
			f1=buffer2[(j-1)*Lie+(i-1)];
			f2=buffer2[(j-1)*Lie+(i+0)];
			f3=buffer2[(j-1)*Lie+(i+1)];
			f4=buffer2[(j+0)*Lie+(i-1)];
			f5=buffer2[(j+0)*Lie+(i+0)];
			f6=buffer2[(j+0)*Lie+(i+1)];
			f7=buffer2[(j+1)*Lie+(i-1)];
			f8=buffer2[(j+1)*Lie+(i+0)];
			f9=buffer2[(j+1)*Lie+(i+1)];
            temp5=h1*f1+h2*f2+h3*f3+h4*f4+h5*f5+h6*f6+h7*f7+h8*f8+h9*f9;

            //temp1=h11*f1+h12*f2+h13*f3+h14*f4+h15*f5+h16*f6+h17*f7+h18*f8+h19*f9;
            //temp2=h21*f1+h22*f2+h23*f3+h24*f4+h25*f5+h26*f6+h27*f7+h28*f8+h29*f9;
            //temp3=h31*f1+h32*f2+h33*f3+h34*f4+h35*f5+h36*f6+h37*f7+h38*f8+h39*f9;
            //temp4=h41*f1+h42*f2+h43*f3+h44*f4+h45*f5+h46*f6+h47*f7+h48*f8+h49*f9;

			//if(temp1>temp2&&temp1>temp3&&temp1>temp4) temp5=temp1;
			//if(temp2>temp1&&temp2>temp3&&temp2>temp4) temp5=temp2;
			//if(temp3>temp1&&temp3>temp2&&temp3>temp4) temp5=temp3;
			//if(temp4>temp1&&temp4>temp2&&temp4>temp3) temp5=temp4;

			if(temp5>255) temp5=255;
			if(temp5<0) temp5=0;

			buffer3[(j+0)*Lie+(i+0)]=temp5;
    	    pDC->SetPixel(i,j,RGB(buffer3[(j+0)*Lie+(i+0)],buffer3[(j+0)*Lie+(i+0)],buffer3[(j+0)*Lie+(i+0)]));
		}
	}
}



~~~



### 10 伪色彩图像增强

~~~C++
#include <afxwin.h> 
#include <afxext.h> 
#include <afxdisp.h>   
#include <afxdtctl.h>
#include "resource.h"

/*文档类*/
class CDoc: public CDocument
{	
	DECLARE_DYNCREATE(CDoc)
};
IMPLEMENT_DYNCREATE(CDoc, CDocument)
/*主帧窗口类*/
class CMainFrame : public CFrameWnd
{	
	DECLARE_DYNCREATE(CMainFrame)
};
IMPLEMENT_DYNCREATE(CMainFrame, CFrameWnd)

/*视图类*/
class CMyView : public CView
{
public:
	//区域一：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓

	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	DECLARE_MESSAGE_MAP()
	DECLARE_DYNCREATE(CMyView)
public:
	CMyView()
	{
	//区域二：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓


	//↑↑↑↑↑↑↑↑↑↑↑自由写代码区域截止线，其它区域请勿作任何修改↑↑↑↑↑
	}
	virtual ~CMyView(){}
	public:
	virtual void OnDraw(CDC* pDC){} 
	protected:                                                          
	afx_msg void On01();                                            
};                                                                  
IMPLEMENT_DYNCREATE(CMyView, CView)                       
BEGIN_MESSAGE_MAP(CMyView, CView)                                                    
	ON_COMMAND(ID01, On01)                                           
END_MESSAGE_MAP()                                                    
                                                                    
/*应用程序类*/                                                      
class CApp : public CWinApp{	virtual BOOL InitInstance(); };
BOOL CApp::InitInstance ()                             
{		CSingleDocTemplate* pDocTemplate= new CSingleDocTemplate(IDR_MENU1,RUNTIME_CLASS(CDoc),
													

RUNTIME_CLASS(CMainFrame),RUNTIME_CLASS(CMyView));
		AddDocTemplate(pDocTemplate);CCommandLineInfo cmdInfo;ParseCommandLine(cmdInfo);
		if (!ProcessShellCommand(cmdInfo))	return FALSE;            
		m_pMainWnd->ShowWindow(SW_SHOW);m_pMainWnd->UpdateWindow();  
		m_pMainWnd->SetWindowPos(NULL,0,0,800,600,0);                
		m_pMainWnd->SetWindowText("版本1.01.");
		return TRUE;                                                 
}                                                                    
CApp theApp;             
                                
//区域三：↓↓↓↓↓↓↓以下区域可自由写代码，其它区域请勿作任何修改↓↓↓↓↓
void CMyView::On01() 
{

	CBitmap hbitmap;         //利用"位图类"声明一个标识符为"hbitmap"的变量
	BITMAP bm;               //利用"位图文件头结构体"声明一个标识符为"bm"的变量
    unsigned char *buffer;            //声明了一个8位整型数"buffer"指针
	int Hang,Lie,WidthBytes;       //声明行总数目，列总数目，单个象素占据的字节个数

	hbitmap.DeleteObject();                                  //位图类的清空位图函数
	hbitmap.LoadBitmap (IDB_BITMAP2);                        //加载位图文件
	hbitmap.GetBitmap (&bm);     //将所加载的位图文件的头文件信息装载到标识符为"bm"的变量中
	Hang=bm.bmHeight,Lie=bm.bmWidth,WidthBytes=bm.bmWidthBytes;
	buffer= new unsigned char[Hang*WidthBytes];    
	//新开辟一段由上述三个变量决定个数的8位整形数组然后被buffer指针所指向
	hbitmap.GetBitmapBits (Hang*WidthBytes, buffer);   //录入buffer数组的位图数据

    //buffer[j*WidthBytes+i*4+k];

	int i,j;
	int tempR,tempG,tempB;

    CDC *pDC;
	pDC=GetDC();


    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));
		}
	}

	MessageBox("以下将RGB图转灰度图");
	int temp;
    unsigned char *buffer2;            //声明了一个8位整型数"buffer"指针
	buffer2= new unsigned char[Hang*Lie];
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			tempR=buffer[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer[j*WidthBytes+i*WidthBytes/Lie+0];
			temp=(tempR+tempG+tempB)/3;
    	    pDC->SetPixel(i,j,RGB(temp,temp,temp));
			buffer2[j*Lie+i]=temp;
		}
	}

	MessageBox("以下进行灰度图的伪彩色图像增强");
	unsigned char TR[256],TG[256],TB[256];
	for(int k=0;k<256;k++)
	{
		TR[k]=2*k-3;
		TG[k]=1.5*k-8;
		TB[k]=2.8*k-15;
	}//以上索引表建立完毕

	unsigned char* buffer3= new unsigned char[Hang*WidthBytes];    
    for(j=0;j<Hang;j++)
	{
		for(i=0;i<Lie;i++)
		{
			k=buffer2[j*Lie+i];

			buffer3[j*WidthBytes+i*WidthBytes/Lie+2]=TR[k];
			buffer3[j*WidthBytes+i*WidthBytes/Lie+1]=TG[k];
			buffer3[j*WidthBytes+i*WidthBytes/Lie+0]=TB[k];

			tempR=buffer3[j*WidthBytes+i*WidthBytes/Lie+2];
			tempG=buffer3[j*WidthBytes+i*WidthBytes/Lie+1];
			tempB=buffer3[j*WidthBytes+i*WidthBytes/Lie+0];
    	    pDC->SetPixel(i,j,RGB(tempR,tempG,tempB));

		}
	}
}



~~~



